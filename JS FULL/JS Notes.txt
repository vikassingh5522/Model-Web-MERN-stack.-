// ================================================================
// 💡 WHY JAVASCRIPT IS IMPORTANT
// ================================================================

// JavaScript is used to make websites interactive and dynamic.
// ✅ It adds animations, interactions, and engagement to web pages.
// ✅ Used for validation, animations, effects, and user engagement.

console.log("Why JS is Important?");
console.log("Because it creates animations, validations, and interactive engagement on websites.");


// ================================================================
// ⚙️ WHAT JAVASCRIPT CAN DO
// ================================================================

// 👉 Engagement (user attraction)
// 👉 Interactions (button clicks, mouse events, etc.)
// 👉 Animations (smooth transitions, movements)
// 👉 Validations (form data checking before submission)

console.log("JavaScript can do: Engagement, Interactions, Animations, and Validations.");


// ================================================================
// 🧩 HOW TO ATTACH JAVASCRIPT TO HTML
// ================================================================

/*
In HTML file:

1️⃣ Internal JavaScript
   <script>
      console.log("Hello from internal JS!");
   </script>

2️⃣ External JavaScript
   <script src='script.js'></script>

JavaScript runs directly in the browser.
*/


// ================================================================
// 🔹 VARIABLES IN JAVASCRIPT
// ================================================================

// 1️⃣ var → old version (function-scoped)
{
    var a = 10;  // accessible outside block
}
console.log("var example:", a);  // ✅ Works fine


// 2️⃣ let → introduced in ES6 (block-scoped)
{
    let b = 20;  // accessible only inside block
    console.log("let inside block:", b);
}
// console.log(b); ❌ ReferenceError: b is not defined


// 3️⃣ const → introduced in ES6 (block-scoped and cannot be reassigned)
{
    const c = 30; 
    console.log("const inside block:", c);
    // c = 40; ❌ TypeError: Assignment to constant variable.
}
// console.log(c); ❌ ReferenceError: c is not defined



// ================================================================
// 🧠 CONSOLE METHODS IN JAVASCRIPT
// ================================================================

console.log("1️⃣ console.log() → General output message");
console.info("2️⃣ console.info() → Informational message");
console.warn("3️⃣ console.warn() → Warning message");
console.error("4️⃣ console.error() → Error message");
console.table({ name: "Vikas", role: "Developer", age: 22 });

/*
✅ Output Explanation:
- console.log → prints normal text
- console.info → prints info text
- console.warn → shows a warning (yellow)
- console.error → shows an error (red)
- console.table → displays data in a table format
*/



// ================================================================
// 💬 PROMPT() AND ALERT() IN JAVASCRIPT
// ================================================================

// prompt() → used to take input from the user
// alert() → used to show a message to the user

// Example (works in browser only):
// let user = prompt("What is your name?");
// alert("Welcome " + user + "!");

// console.log("User entered:", user);

// Note 🔥
// Both double quotes (" ") and single quotes (' ') can be used for strings
let msg1 = "Hello, World!";
let msg2 = 'JavaScript is fun!';
console.log(msg1, msg2);



// ================================================================
// 🔤 STRING METHODS AND TEMPLATE LITERALS
// ================================================================

let str = "Hello JavaScript World!";

console.log("1️⃣ slice() →", str.slice(0, 5)); // "Hello"
console.log("2️⃣ split(' ') →", str.split(" ")); // ["Hello", "JavaScript", "World!"]
console.log("3️⃣ replace('World', 'Universe') →", str.replace("World", "Universe")); // replaces
console.log("4️⃣ includes('JavaScript') →", str.includes("JavaScript")); // true
console.log("5️⃣ toUpperCase() →", str.toUpperCase()); // HELLO JAVASCRIPT WORLD!
console.log("6️⃣ toLowerCase() →", str.toLowerCase()); // hello javascript world!



/* ================================================================
🧾 TEMPLATE LITERALS (BACKTICKS ` `)
================================================================== */

// Template literals → allow embedding variables and expressions easily
let name = "Vikas";
let age = 22;
console.log(`My name is ${name} and I am ${age} years old.`); // ✅
console.log(`Next year I will be ${age + 1}.`);
console.log(`2 + 3 = ${2 + 3}`);

// Multi-line string using backticks
let intro = `
Hello Everyone,
My name is ${name}.
I love JavaScript and coding!
`;
console.log(intro);



// ================================================================
// 🧮 STATEMENTS AND EXPRESSIONS
// ================================================================

// Expression → produces a value
let sum = 10 + 20; // 30 is an expression result
console.log("Sum is:", sum);

// Statement → performs an action
if (sum > 20) {
    console.log("Statement executed: sum is greater than 20");
}

/*
👉 Expression = Something that produces a value
👉 Statement = A complete instruction that performs an action
*/



// ================================================================
// ✅ SUMMARY
// ================================================================

/*
1️⃣ var → old, function-scoped
2️⃣ let → block-scoped (ES6)
3️⃣ const → block-scoped, constant (ES6)
4️⃣ console methods → log(), info(), warn(), error(), table()
5️⃣ prompt() and alert() → user input and messages
6️⃣ Strings → " " or ' '
7️⃣ String methods → slice(), split(), replace(), includes(), toUpperCase(), toLowerCase()
8️⃣ Template literals → `Backticks` used to embed variables and expressions
9️⃣ Statements vs Expressions → action vs value
*/
